package com.armystartup.domain.schedule.entity;

public enum RepeatType {
    NONE,       // 반복 없음
    DAILY,      // 매일
    WEEKDAY,    // 평일마다
    WEEKLY,     // 매주 같은 요일
    MONTHLY     // 매월 같은 날
}
